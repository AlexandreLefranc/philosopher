#include "philo.h"

void	boss_job(/*fork_tab, philo_tab*/)
{
	while (running == 1 || all_philo_done(philo_tab) != 1)
	{
		// check_dead_philo();
		// if (any_dead_philo == 1)
			// running == 0;
	}
	// detach all threads
	// free all
}
